/**
 * Placeholder migration entrypoint — implemented in Phase 1 (plan).
 *
 * Applies ordered SQL files from db/migrations/ against DATABASE_URL using pg.
 */
console.error(
  "[db:migrate] Migration runner is not wired yet — see plan Phase 1.",
);
process.exit(1);
